# Rime 默认方案与行为（Fcitx5）
# 使用朙月拼音·简化字，默认输出简体中文
patch:
  schema_list:
    - schema: luna_pinyin_simp   # 朙月拼音·简化字（简体）
    - schema: emoji
  menu:
    page_size: 6
  ascii_composer/switch_key:
    Shift_L: commit_code
    Shift_R: commit_code
